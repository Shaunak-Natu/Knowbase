# KnowBase Environment Variables
# Copy this file to .env and edit before running docker compose

# Port the app is exposed on (default: 3001)
APP_PORT=3001

# Your public URL (used for CORS). Change to your domain in production.
CLIENT_URL=http://localhost:3001

# Set to 'production' — already set in docker-compose, here for reference
NODE_ENV=production
